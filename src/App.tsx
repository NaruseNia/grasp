import "./App.css";
import { Button } from "@adobe/react-spectrum";

export const App = () => {
  return (
    <main className="container w-screen h-screen grid place-items-center">
      <Button variant="accent">
        Hello
      </Button>
    </main>
  );
};

